# findmeparking

To run, install necessary libraries using 

```sh
pip install -r requirements.txt
```

Add a file `secret.py` with the 

```
TELEGRAM_TOKEN = "???"
DATAMALL_APIKEY = "???"
GOOGLE_MAPS_APIKEY = "???"
```

Then run 

```sh
python bot.py
```